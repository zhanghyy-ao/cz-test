# 项目思考与过程记录

本文档用于记录每一次对项目数据、方法、实验、问题和决策的观察。后续开发时，所有重要判断都应尽量在这里留下可追溯记录。

## 记录模板

每次新增记录建议使用以下格式：

```markdown
## YYYY-MM-DD HH:mm - 主题

### 本次目标

- 

### 数据观察

- 数据来源：
- 数据形态：
- 关键字段：
- 异常或注意点：

### 方法与实现

- 使用方式：
- 修改文件：
- 关键逻辑：

### 验证结果

- 执行命令：
- 结果摘要：
- 问题记录：

### 思考与下一步

- 当前判断：
- 后续计划：
```

## 2026-05-07 - 初始化文档体系

### 本次目标

- 创建 `doc` 文档目录。
- 建立项目规则文档 `PRD.md`。
- 建立过程记录文档 `think.md`。

### 数据观察

- 当前尚未开始正式读取和分析数据。
- 项目根目录存在 `data/`、基础 Python 文件、需求文件和项目说明 PDF。

### 方法与实现

- 使用 `doc/PRD.md` 记录项目规则、范围、开发约定和交付标准。
- 使用 `doc/think.md` 持续记录后续每次数据观察、实现方式、验证结果和思考路径。

### 验证结果

- 已创建 `doc` 目录。
- 已新增 `PRD.md` 与 `think.md`。

### 思考与下一步

- 下一步应阅读项目说明 PDF 与核心代码，明确任务目标、输入输出、评测方式和需要实现的位置。

## 2026-05-07 - 配置远端仓库与测试前提交规则

### 本次目标

- 将本地项目内容放入 GitHub 仓库 `https://github.com/zhanghyy-ao/cz-test.git`。
- 在 PRD 中增加“每一次测试前必须提交一次”的规则。

### 数据观察

- 当前项目目录原本不是 Git 仓库。
- 目标远端仓库由用户提供。

### 方法与实现

- 已在 `doc/PRD.md` 的验证规则中加入测试前提交要求。
- 后续将初始化 Git 仓库、关联远端、提交当前项目内容并推送。

### 验证结果

- 待完成远端仓库检查、提交与推送。

### 思考与下一步

- 推送前需要确认 GitHub CLI 可用且已经登录。
- 若远端仓库已有提交，需要先获取远端状态，避免覆盖已有内容。

## 2026-05-07 - 阅读项目并补全 PRD

### 本次目标

- 阅读项目说明 PDF、核心代码、数据、tokenizer 配置和已有文档。
- 将真实任务需求、接口约束、评测规则和交付标准写入 `doc/PRD.md`。

### 数据观察

- 数据来源：`data/train_dev.jsonl`、`data/test_dev.jsonl` 及对应 `(1)` 重复文件。
- 数据形态：JSONL，每行包含 `text` 和 `label`。
- 关键字段：`text` 为待分类文本，`label` 为 exact match 目标标签。
- DEV 训练集：231 条，77 类，每类 3 条。
- DEV 验证集：539 条，77 类，每类 7 条。
- 注意点：正式评测包含同域任务、OOD 分类任务和自然语言选择题任务，不能过拟合当前客服意图 DEV 集。

### 方法与实现

- 使用 PDF 提取工具阅读 `Harness Engineering 考核说明（2026年夏）.pdf`。
- 阅读 `solution.py`、`harness_base.py`、`run.py`、`llm_client.py`、`requirements.txt`、`tokenizer/tokenizer_config.json` 和 `tokenizer/chat_template.jinja`。
- 修改文件：`doc/PRD.md`。
- 关键逻辑：将项目目标、范围、关键文件、数据需求、功能需求、接口约束、代码约束、本地运行、评测标准、交付物、验证规则、验收标准和风险对策补入 PRD。

### 验证结果

- 执行命令：`rg --files`、`sed`、`python3` 数据统计、`pypdf` PDF 文本提取、`git status --short --branch`。
- 结果摘要：已确认 `python` 命令不可用但 `python3` 可用；当前目录不是 Git 仓库；尚未配置 LLM API，因此未运行 `run.py` 评测。
- 问题记录：目录中存在 `.DS_Store`、重复数据文件、重复 tokenizer 配置文件和一个百度云下载中的 tokenizer 临时文件，正式提交前需要确认是否清理。

### 思考与下一步

- 当前判断：项目核心是实现可泛化的 `MyHarness`，重点在记忆组织、候选检索、Prompt 预算、输出清洗和 Prompt Injection 防护。
- 后续计划：如继续实现，应先初始化或关联 Git 仓库以满足“测试前提交”规则，然后设计第一版 Harness 并跑 DEV 验证。
